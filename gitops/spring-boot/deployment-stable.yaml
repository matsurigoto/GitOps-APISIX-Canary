# =============================================================================
# Spring Boot — Stable Deployment (v1)
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-boot-stable
  namespace: app
  labels:
    app: spring-boot
    version: stable
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring-boot
      version: stable
  template:
    metadata:
      labels:
        app: spring-boot
        version: stable
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: spring-boot
          # ⚠ Replace <ACR_LOGIN_SERVER> with your actual ACR login server
          image: acrgitopscanaryc31ba2.azurecr.io/spring-demo:v1
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: APP_VERSION
              value: "v1"
            - name: OTEL_SERVICE_NAME
              value: "demo-api-stable"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.observability:4318"
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: "http/protobuf"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.version=v1,deployment.environment=production"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
